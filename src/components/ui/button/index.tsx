import DangerButton from './danger-button';
import SecondaryButton from './secondary-button';
import PrimaryButton from './primary-button';

export { DangerButton, SecondaryButton, PrimaryButton };

// Generic Button component can be added here later if needed.
// For now, we export DangerButton as requested.
export default DangerButton;
